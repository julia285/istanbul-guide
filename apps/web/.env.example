# Same DB the pipeline writes to — see packages/db/.env.example
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/istanbul_guide?schema=public"

# Filled in once a Supabase project exists (see architecture doc section 9 for why)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
